{
  "name": "universalvideomaximizer",
  "version": "3.1.202",
  "license": "MIT",
  "bugs": "https://github.com/trophygeek/universalvideomaximizer/issues",
  "npmClient": "yarn",
  "dependencies": {},
  "devDependencies": {
    "@rollup/plugin-commonjs": "^25.0.7",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-replace": "^5.0.5",
    "@rollup/plugin-typescript": "^11.1.5",
    "@types/chrome": "^0.0.268",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.9",
    "cross-env": "^7.0.3",
    "glob": "^7.2.3",
    "jest": "^29.7.0",
    "magic-string": "^0.30.10",
    "prettier": "^2.8.8",
    "rimraf": "^3.0.2",
    "rollup": "^4.6.0",
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-empty-dir": "^1.0.5",
    "rollup-plugin-inline-js": "^0.5.0",
    "rollup-plugin-zip": "^1.0.3",
    "ts-jest": "^29.1.1",
    "typescript": "^5.5.2",
    "vite": "^5.3.2"
  },
  "browserslist": {
    "production": [
      ">0.2%"
    ],
    "development": [
      "last 1 chrome version"
    ]
  },
  "scripts": {
    "start": "yarn build:dev",
    "build": "NODE_ENV=production rollup -c rollup.config.mjs --environment BUILD:production",
    "build:dev": "NODE_ENV=development rollup -w -c rollup.config.mjs --environment BUILD:development",
    "clean": "rimraf dist/videomaximizer",
    "test": "jest --force-exit",
    "lint:fix": "prettier --bracket-same-line  --print-width 100 --write \"src/**/*.{ts,tsx}\"",
    "update": "git checkout -- yarn.lock && yarn install && yarn upgrade-interactive --lastest",
    "buildpackage": "yarn build:prod && node ./scripts/bundlepackage.js"
  },
  "sideEffects": false
}
